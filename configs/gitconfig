# You can view the commands in this .gitconfig file with: git config --list
#
#	reminder: GitHub account uses 2FA - so use specific generated password
#	when logging in from the command line - do not the GitHub password!
#
[core]
	editor = emacs
	pager = less -r
[user]
	name = Simon Rowe
	email = simon@wiremoons.com
[color]
	ui = true
[alias]
	graph = log --graph --oneline --decorate --stat
[credential]
	helper = cache --timeout=7200
[push]
	default = simple
